# Port exposé sur la machine hôte par `make docker-dev` (le conteneur écoute toujours sur 8080 en interne).
PORT=8091
HTTP_ADDR=127.0.0.1:8080
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require